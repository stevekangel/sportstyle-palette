export const metadata = {
  title: "Sportstyle Palette",
  description: "ASICS GEL-Cumulus 16 Color Match Tool",
};

export default function RootLayout({ children }) {
  return (
    <html lang="en">
      <body style={{ margin: 0, padding: 0 }}>{children}</body>
    </html>
  );
}
